# Copy to ".env" in this folder, set your Web client ID, then restart: npm run dev
# Google Cloud → Credentials → OAuth 2.0 Client ID (Web) → Authorized JavaScript origins:
#   http://localhost:5173
#   (add your production URL when you deploy)
VITE_GOOGLE_CLIENT_ID=
